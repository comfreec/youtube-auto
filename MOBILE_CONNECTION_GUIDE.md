# 📱 모바일 클라이언트 접속 가이드

## 🎯 접속 주소 요약

### 클라이언트 접속 (모바일에서)
```
http://[컴퓨터IP]:8501
```

### API 서버 주소 (클라이언트에서 설정)
```
http://[컴퓨터IP]:8001
```

## 📋 단계별 접속 방법

### 1단계: 컴퓨터 IP 확인
```bash
# IP 확인 스크립트 실행
python check_ip.py
```

**또는 수동으로 확인:**
- Windows: `ipconfig` 명령어
- Mac/Linux: `ifconfig` 명령어
- 일반적으로 `192.168.1.xxx` 형태

### 2단계: 서버 실행
```bash
# 통합 실행 (권장)
python mobile_setup.py

# 또는 개별 실행
python start_mobile_server.py    # API 서버
python start_mobile_client.py    # 클라이언트 앱
```

### 3단계: 모바일에서 접속
1. **모바일과 컴퓨터를 같은 WiFi에 연결**
2. **모바일 브라우저 열기** (Chrome, Safari 등)
3. **주소창에 입력**: `http://192.168.1.100:8501` (예시)
4. **접속 확인**

### 4단계: 서버 연결 설정
1. **"서버 연결 설정" 확장**
2. **API 서버 주소 입력**: `http://192.168.1.100:8001`
3. **"연결" 버튼 클릭**
4. **"서버 연결 성공!" 메시지 확인**

### 5단계: 영상 생성
1. **영상 주제 입력**
2. **설정 선택** (타입, 언어, 길이 등)
3. **"영상 생성 시작" 버튼 클릭**
4. **실시간 진행 상황 확인**

## 🌐 실제 접속 예시

### 컴퓨터 IP가 192.168.1.100인 경우:
- **모바일 접속**: `http://192.168.1.100:8501`
- **API 서버**: `http://192.168.1.100:8001`

### 컴퓨터 IP가 192.168.0.50인 경우:
- **모바일 접속**: `http://192.168.0.50:8501`
- **API 서버**: `http://192.168.0.50:8001`

## 🔧 포트 설명

| 포트 | 용도 | 접속 대상 |
|------|------|-----------|
| 8501 | 모바일 클라이언트 웹앱 | 모바일 브라우저 |
| 8001 | API 서버 (영상 처리) | 클라이언트 앱 내부 |

## 🚨 문제 해결

### 접속이 안될 때:
1. **같은 WiFi 확인**: 모바일과 컴퓨터가 같은 네트워크인지
2. **IP 주소 확인**: `python check_ip.py`로 정확한 IP 확인
3. **방화벽 설정**: Windows 방화벽에서 포트 허용
4. **서버 실행 확인**: 컴퓨터에서 서버가 실행 중인지
5. **브라우저 캐시**: 모바일 브라우저 캐시 삭제

### 연결은 되지만 서버 연결 실패:
1. **API 서버 주소 확인**: `http://[IP]:8001` 형태로 정확히 입력
2. **서버 상태 확인**: 컴퓨터에서 API 서버 실행 상태 확인
3. **포트 충돌**: 다른 프로그램이 8001 포트 사용 중인지 확인

## 📱 모바일 브라우저별 팁

### Chrome (Android/iOS):
- 주소창에 직접 입력
- 북마크 추가 권장
- 홈 화면에 추가 가능

### Safari (iOS):
- 공유 버튼 > 홈 화면에 추가
- PWA처럼 사용 가능

### Samsung Internet:
- 메뉴 > 홈 화면에 추가
- 다크 모드 지원

## 🔒 보안 고려사항

### 안전한 사용:
- **로컬 네트워크만**: 외부에서 접근 불가
- **방화벽 보호**: 외부 접근 차단
- **임시 사용**: 필요시에만 서버 실행

### 주의사항:
- **공용 WiFi 피하기**: 집/사무실 WiFi만 사용
- **HTTPS 미지원**: 로컬 네트워크에서만 사용
- **포트 노출 최소화**: 사용 후 서버 종료

## 💡 편의 기능

### QR 코드 접속:
```bash
# QR 코드 생성 (qrcode 패키지 필요)
pip install qrcode[pil]
python mobile_setup.py  # QR 코드 자동 생성
```

### 북마크 추천:
- 모바일 브라우저에 북마크 저장
- 홈 화면에 바로가기 추가
- PWA로 설치하여 앱처럼 사용

---

**🎬 이제 모바일에서 편리하게 AI 영상을 생성하세요!**