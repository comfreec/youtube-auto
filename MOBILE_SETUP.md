# 📱 AI 영상 생성 모바일 시스템

컴퓨터를 서버로 하고 모바일을 클라이언트로 하는 클라이언트-서버 구조로 모바일 부하를 완전히 제거한 영상 생성 시스템입니다.

## 🎯 시스템 구조

```
[모바일 클라이언트] ←→ [컴퓨터 API 서버]
     (가벼운 UI)        (모든 영상 처리)
```

- **모바일**: 단순 UI만 담당 (부하 제로)
- **컴퓨터**: 모든 AI 처리, 영상 생성, 업로드 담당

## 🚀 빠른 시작

### 1. 통합 실행 (권장)
```bash
python mobile_setup.py
```
- 선택 1번: 서버 + 클라이언트 동시 실행
- 자동으로 IP 주소와 포트 설정
- QR 코드 생성으로 모바일 접속 간편화

### 2. 개별 실행

#### API 서버 실행 (컴퓨터)
```bash
python start_mobile_server.py
```

#### 클라이언트 실행 (모바일 접속용)
```bash
python start_mobile_client.py
```

## 📱 모바일 연결 방법

### 1단계: 네트워크 확인
- 모바일과 컴퓨터가 **같은 WiFi**에 연결되어 있는지 확인

### 2단계: 모바일에서 접속
- 모바일 브라우저에서 접속: `http://[컴퓨터IP]:8501`
- 예: `http://192.168.1.100:8501`

### 3단계: 서버 연결 설정
- 클라이언트에서 "서버 연결 설정" 확장
- API 서버 주소 입력: `http://[컴퓨터IP]:8001`
- "연결" 버튼 클릭하여 연결 확인

### 4단계: 영상 생성
- 단일 영상 또는 배치 영상 생성 선택
- 영상 주제 입력 후 생성 시작
- 모바일에서 실시간 진행 상황 확인

## 🔧 네트워크 설정

### Windows 방화벽 설정
1. 제어판 > 시스템 및 보안 > Windows Defender 방화벽
2. "고급 설정" 클릭
3. "인바운드 규칙" > "새 규칙"
4. 포트 8001, 8501 허용 규칙 추가

### 라우터 설정
- AP 격리(Client Isolation) 비활성화
- 게스트 네트워크가 아닌 메인 네트워크 사용

## 📋 API 엔드포인트

### 영상 생성
- `POST /api/video/generate` - 단일 영상 생성
- `POST /api/video/batch` - 배치 영상 생성

### 작업 관리
- `GET /api/task/{task_id}/status` - 작업 상태 조회
- `GET /api/task/{task_id}/download` - 영상 다운로드
- `GET /api/tasks` - 모든 작업 목록
- `DELETE /api/task/{task_id}` - 작업 취소

### 시스템
- `GET /health` - 서버 상태 확인
- `GET /` - API 정보

## 🎬 지원 기능

### 영상 타입
- **쇼츠**: 30-90초 짧은 영상
- **롱폼**: 5-15분 긴 영상  
- **타이머**: 5-60분 타이머 영상

### 언어 지원
- 🇰🇷 한국어 (ko-KR)
- 🇺🇸 영어 (en-US)

### 추가 기능
- 자동 YouTube 업로드
- 실시간 진행 상황 모니터링
- 배치 처리 (최대 20개)
- 영상 다운로드
- 작업 이력 관리

## 🔍 문제 해결

### 연결 안됨
1. 같은 WiFi 네트워크 확인
2. 방화벽 설정 확인
3. IP 주소 정확성 확인
4. 포트 충돌 확인

### 서버 시작 실패
```bash
# 포트 사용 확인
netstat -an | findstr :8001
netstat -an | findstr :8501

# 프로세스 종료
taskkill /f /im python.exe
```

### 모바일 접속 안됨
1. 브라우저 캐시 삭제
2. 시크릿/프라이빗 모드 시도
3. 다른 브라우저 시도
4. 모바일 데이터 대신 WiFi 사용

## 📊 성능 최적화

### 모바일 부하 제로
- 모든 AI 처리는 컴퓨터에서
- 모바일은 UI와 상태 확인만
- 네트워크 대역폭 최소 사용

### 서버 성능
- 멀티스레딩으로 동시 처리
- 백그라운드 작업 큐
- 메모리 효율적 관리

## 🛡️ 보안 고려사항

### 네트워크 보안
- 로컬 네트워크 내에서만 접근
- HTTPS 미지원 (로컬 사용)
- 방화벽으로 외부 접근 차단

### 데이터 보안
- 영상 파일은 컴퓨터에만 저장
- API 키는 서버에서만 사용
- 모바일에 민감 정보 저장 안함

## 📈 확장 가능성

### 클라우드 배포
- AWS, GCP 등 클라우드 서버 배포 가능
- Docker 컨테이너화 지원
- 로드 밸런싱 적용 가능

### 다중 클라이언트
- 여러 모바일에서 동시 접속
- 사용자별 작업 분리
- 권한 관리 시스템

## 🆘 지원

문제가 발생하면:
1. 로그 파일 확인
2. 네트워크 연결 상태 확인
3. 서버/클라이언트 재시작
4. 방화벽 설정 재확인

---

**💡 팁**: QR 코드를 사용하면 모바일에서 더 쉽게 접속할 수 있습니다!