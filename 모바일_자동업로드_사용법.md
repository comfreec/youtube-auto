# 📱 모바일 자동 업로드 사용법

## 🎯 이 방법을 사용하는 이유

- ✅ **백그라운드 워커 작동**: 화면을 꺼도 영상 생성 계속됨
- ✅ **자동 YouTube 업로드**: 한국어 + 영어 버전 모두 자동 업로드
- ✅ **모바일 접속 가능**: ngrok으로 외부에서 접속

## 🚀 시작 방법

### 1단계: 서버 시작

```bash
모바일_자동업로드_시작.bat
```

이 배치 파일을 실행하면:
1. 기존 프로세스 종료 (python, ngrok)
2. 모바일 API 서버 시작 (포트 8001)
3. ngrok 터널 생성 (포트 8001)

### 2단계: ngrok 주소 확인

터미널에 다음과 같이 표시됩니다:

```
Forwarding  https://xxxx-xxxx-xxxx.ngrok-free.app -> http://localhost:8001
```

이 주소를 복사하세요!

### 3단계: 모바일에서 접속

1. 모바일 브라우저 열기
2. ngrok 주소 입력 (예: `https://xxxx-xxxx-xxxx.ngrok-free.app`)
3. 보라색 그라데이션 UI가 나타남

### 4단계: 영상 생성

1. "영상 주제" 입력 (예: "건강한 아침 루틴 만들기")
2. "🚀 영상 생성 시작" 버튼 클릭
3. "✅ 생성 시작!" 메시지 확인
4. **화면을 꺼도 됩니다!** 백그라운드에서 계속 작업합니다

## 📊 작동 과정

```
모바일 요청
    ↓
백그라운드 워커에 작업 제출
    ↓
한국어 영상 생성 → YouTube 자동 업로드
    ↓
영어 영상 생성 → YouTube 자동 업로드
    ↓
완료!
```

## ⚠️ 중요 사항

### 포트 구분

- **8001 포트** (모바일 API 서버): 백그라운드 워커 + 자동 업로드 ✅
- **8501 포트** (Streamlit 서버): 일반 UI, 자동 업로드 안 됨 ❌

**반드시 8001 포트로 접속해야 합니다!**

### ngrok 에러 해결

만약 다음 에러가 나타나면:

```
ERROR: The endpoint is already online
```

해결 방법:
1. 모든 터미널 창 닫기
2. 작업 관리자에서 `ngrok.exe` 프로세스 종료
3. `모바일_자동업로드_시작.bat` 다시 실행

### 서버 확인

서버가 제대로 시작되었는지 확인:

```bash
netstat -an | findstr ":8001"
```

출력이 있으면 서버가 실행 중입니다.

## 🔍 문제 해결

### 1. ngrok 주소로 접속이 안 됨

- ngrok 터미널 창이 열려 있는지 확인
- ngrok 주소를 정확히 복사했는지 확인
- 모바일이 인터넷에 연결되어 있는지 확인

### 2. 서버가 시작되지 않음

```bash
# 포트 8001이 사용 중인지 확인
netstat -an | findstr ":8001"

# 사용 중이면 프로세스 종료
taskkill /F /IM python.exe
```

### 3. 자동 업로드가 안 됨

- `token.pickle` 파일이 있는지 확인
- `client_secrets.json` 파일이 있는지 확인
- YouTube API 인증이 되어 있는지 확인

## 📝 로그 확인

백그라운드 작업 상태 확인:

```bash
python check_background_status.py
```

## 🎉 성공 확인

1. 모바일에서 영상 생성 요청
2. "✅ 생성 시작!" 메시지 확인
3. Task ID 확인
4. 화면 종료
5. 나중에 YouTube 채널에서 업로드된 영상 확인!

## 💡 팁

- 여러 영상을 한 번에 생성하려면 여러 번 요청하세요
- 각 영상은 독립적으로 백그라운드에서 처리됩니다
- 한국어 + 영어 버전이 자동으로 생성됩니다
- 모바일 화면을 꺼도 PC에서 계속 작업합니다

## 🔄 서버 종료

ngrok 터미널에서 `Ctrl+C`를 누르면 서버가 종료됩니다.
