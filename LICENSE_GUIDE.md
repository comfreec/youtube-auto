# 라이선스 시스템 사용 가이드

## 📋 개요

이 프로그램은 라이선스 키 기반 인증 시스템을 사용합니다.
- 라이선스 키 + 하드웨어 ID 바인딩 방식
- 한 대의 컴퓨터에서만 사용 가능
- 만료일 설정 가능

---

## 🔑 판매자용 가이드

### 1. 라이선스 키 생성

```bash
python generate_license.py
```

**입력 정보:**
- 고객 이름 (선택사항)
- 유효 기간 (일 단위, 기본 365일)

**출력:**
```
📋 라이선스 키: ABCD-1234-EFGH-5678
👤 고객 이름: 홍길동
📅 유효 기간: 365일
```

### 2. 라이선스 키 전달

생성된 라이선스 키를 고객에게 전달합니다.

---

## 👤 구매자용 가이드

### 1. 프로그램 실행

```bash
streamlit run webui/Main.py
```

### 2. 라이선스 활성화

프로그램 첫 실행 시 라이선스 활성화 화면이 표시됩니다.

1. 판매자로부터 받은 라이선스 키 입력
2. "🔓 활성화" 버튼 클릭
3. 활성화 완료!

### 3. 라이선스 정보 확인

- 사이드바에서 라이선스 유효 기간 확인
- 만료 30일 전부터 경고 표시

---

## 🔒 보안 기능

### 하드웨어 ID 바인딩

- 최초 활성화 시 컴퓨터의 하드웨어 ID와 연동
- 다른 컴퓨터에서 사용 불가
- 하드웨어 변경 시 재활성화 필요

### 암호화 저장

- 라이선스 정보는 암호화되어 `license.dat` 파일에 저장
- 파일 수정 시 자동으로 무효화

### 만료일 관리

- 설정된 기간 이후 자동 만료
- 만료 전 갱신 가능

---

## ⚙️ 고급 설정

### 암호화 키 변경 (배포 전 필수!)

`app/services/license.py` 파일에서:

```python
self.secret_key = b'your-secret-key-change-this-to-random-32-bytes-key'
```

이 키를 랜덤한 32바이트 키로 변경하세요.

**키 생성 방법:**
```python
from cryptography.fernet import Fernet
print(Fernet.generate_key())
```

---

## 🛠️ 문제 해결

### 라이선스 파일 손상

```bash
# license.dat 파일 삭제 후 재활성화
rm license.dat
```

### 하드웨어 변경 후 사용 불가

판매자에게 연락하여 새 라이선스 키 발급 요청

### 만료일 연장

판매자에게 연락하여 새 라이선스 키 발급 요청

---

## 📞 지원

라이선스 관련 문의는 판매자에게 연락하세요.

---

## ⚠️ 주의사항

1. **라이선스 키 공유 금지**
   - 라이선스 키는 개인용입니다
   - 다른 사람과 공유하지 마세요

2. **license.dat 파일 백업**
   - 라이선스 파일을 안전하게 보관하세요
   - 삭제 시 재활성화 필요

3. **하드웨어 변경 주의**
   - 주요 하드웨어 변경 시 재활성화 필요
   - 변경 전 판매자에게 문의 권장

---

## 📊 라이선스 정보 확인

프로그램 실행 중:
- 사이드바에서 남은 기간 확인
- 만료 30일 전부터 경고 표시
- 만료 시 자동으로 사용 중단
