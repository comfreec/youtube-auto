# 판매자용 배포 가이드

## 📦 고객에게 제공할 파일 준비

### 1단계: 불필요한 파일 제거

다음 파일/폴더는 고객에게 제공하지 않습니다:

```
제거할 파일/폴더:
- .git/
- __pycache__/
- venv/
- storage/
- debug_*.py
- test_*.py
- license.dat (개발자 라이선스)
- license_database.json (라이선스 생성 기록)
- token.pickle, token_timer.pickle (YouTube 인증)
- client_secrets.json (YouTube 인증)
- .vscode/
- build/, dist/ (PyInstaller 빌드 파일)
```

### 2단계: 필수 파일 확인

고객에게 제공할 파일:

```
필수 파일:
✅ setup_for_customer.bat (자동 설치)
✅ 프로그램실행.bat (프로그램 실행)
✅ 외부접속.bat (모바일 접속)
✅ 고객용_README.md (사용 설명서)
✅ requirements.txt (패키지 목록)
✅ config.example.toml (설정 예제)
✅ app/ (프로그램 코드)
✅ webui/ (웹 UI)
✅ resource/ (리소스 파일)
✅ ngrok.exe (모바일 접속용)
```

### 3단계: 배포 패키지 생성

#### 방법 1: 수동 복사

1. 새 폴더 생성 (예: `AI쇼츠생성기_v1.0`)
2. 필수 파일만 복사
3. ZIP으로 압축

#### 방법 2: 자동 스크립트 (추천)

`create_distribution.bat` 파일 생성:

```batch
@echo off
echo 배포 패키지 생성 중...

set DIST_FOLDER=AI쇼츠생성기_배포판
set VERSION=v1.0

REM 기존 폴더 삭제
if exist "%DIST_FOLDER%" rmdir /s /q "%DIST_FOLDER%"

REM 새 폴더 생성
mkdir "%DIST_FOLDER%"

REM 필수 파일 복사
xcopy /E /I app "%DIST_FOLDER%\app"
xcopy /E /I webui "%DIST_FOLDER%\webui"
xcopy /E /I resource "%DIST_FOLDER%\resource"

copy setup_for_customer.bat "%DIST_FOLDER%\"
copy 프로그램실행.bat "%DIST_FOLDER%\"
copy 외부접속.bat "%DIST_FOLDER%\"
copy 고객용_README.md "%DIST_FOLDER%\README.md"
copy requirements.txt "%DIST_FOLDER%\"
copy config.example.toml "%DIST_FOLDER%\"
copy ngrok.exe "%DIST_FOLDER%\"
copy LICENSE "%DIST_FOLDER%\"

REM ZIP 압축 (7-Zip 필요)
"C:\Program Files\7-Zip\7z.exe" a -tzip "AI쇼츠생성기_%VERSION%.zip" "%DIST_FOLDER%"

echo 완료! AI쇼츠생성기_%VERSION%.zip 파일이 생성되었습니다.
pause
```

## 🔑 라이선스 키 생성 및 전달

### 1. 라이선스 키 생성

1. `dist/라이선스생성기.exe` 실행
2. 고객 정보 입력:
   - 고객 이름
   - 유효 기간 (일)
   - 메모 (선택)
3. "라이선스 키 생성" 버튼 클릭
4. 생성된 키 복사

### 2. 고객에게 전달

**전달 내용:**
```
안녕하세요!

AI 쇼츠 생성기를 구매해주셔서 감사합니다.

📦 프로그램 다운로드:
[다운로드 링크 또는 첨부 파일]

🔑 라이선스 키:
XXXX-XXXX-XXXX-XXXX

📖 설치 방법:
1. 다운로드한 ZIP 파일 압축 해제
2. "고객용_README.md" 파일 참고
3. setup_for_customer.bat 실행
4. 프로그램실행.bat 실행
5. 라이선스 키 입력

⚠️ 주의사항:
- 라이선스는 1대의 PC에만 사용 가능
- 유효기간: [만료일]
- 라이선스 키는 타인과 공유 금지

문의사항이 있으시면 언제든 연락주세요!
```

## 📋 고객 관리

### 라이선스 기록 관리

`license_database.json` 파일에 모든 라이선스 정보가 저장됩니다:
- 고객명
- 라이선스 키
- 유효기간
- 생성일
- 메모

### 라이선스 연장

1. 라이선스생성기에서 새 키 생성
2. 유효기간을 연장 기간으로 설정
3. 고객에게 새 키 전달
4. 고객이 프로그램에서 새 키로 재인증

### 문제 해결 지원

**고객이 자주 묻는 질문:**

1. **라이선스 인증 실패**
   - 키를 정확히 입력했는지 확인
   - 인터넷 연결 확인
   - 만료일 확인

2. **PC 변경 시**
   - 새 PC에서는 새 라이선스 필요
   - 또는 기존 라이선스 이전 (하드웨어 ID 변경)

3. **프로그램 오류**
   - Python 버전 확인 (3.10 이상)
   - 재설치 시도
   - 로그 파일 확인

## 💰 가격 정책 예시

```
기본 라이선스:
- 1개월: 30,000원
- 3개월: 80,000원 (11% 할인)
- 6개월: 150,000원 (17% 할인)
- 1년: 250,000원 (31% 할인)

추가 옵션:
- PC 추가: +50%
- 기업용 (무제한): 별도 문의
```

## 🔄 업데이트 배포

### 버전 업데이트 시

1. 새 배포 패키지 생성
2. 버전 번호 변경 (v1.0 → v1.1)
3. 변경사항 문서 작성
4. 고객에게 공지:
   ```
   [업데이트 안내]
   
   버전: v1.1
   날짜: 2025-02-13
   
   변경사항:
   - 새 기능 추가
   - 버그 수정
   - 성능 개선
   
   업데이트 방법:
   1. 기존 폴더 백업
   2. 새 파일 다운로드
   3. 압축 해제 후 덮어쓰기
   4. setup_for_customer.bat 재실행
   
   기존 라이선스는 그대로 사용 가능합니다.
   ```

## 📊 판매 통계

라이선스생성기에서 확인 가능:
- 총 생성 라이선스 수
- 유효 라이선스 수
- 만료 예정 라이선스
- 고객별 라이선스 현황

## ⚠️ 보안 주의사항

1. **라이선스생성기 보안**
   - `dist/라이선스생성기.exe`는 절대 공유 금지
   - `license_database.json` 백업 필수

2. **암호화 키 보안**
   - `app/services/license.py`의 암호화 키 변경 금지
   - 소스 코드 공개 금지

3. **개발자 라이선스**
   - `license.dat` 파일 백업
   - PC 포맷 전 반드시 백업

## 📞 고객 지원 템플릿

### 설치 문제
```
안녕하세요!

설치 중 문제가 발생하셨군요.
다음 사항을 확인해주세요:

1. Python 3.10 이상 설치 확인
2. "Add Python to PATH" 옵션 체크 확인
3. 안티바이러스 프로그램 일시 중지
4. setup_for_customer.bat 관리자 권한으로 실행

여전히 문제가 있으시면:
- 오류 메시지 스크린샷
- Windows 버전
위 정보를 보내주세요.
```

### 라이선스 문제
```
라이선스 관련 문의 감사합니다.

현재 라이선스 상태:
- 고객명: [이름]
- 유효기간: [기간]
- 만료일: [날짜]
- 상태: [유효/만료]

[해결 방법 안내]
```

---

**성공적인 판매를 기원합니다! 💪**
